#include<stdio.h>
int main()
{
    int i,x=10,y=100%90;
    for(i=1;i<=10;i++)
    {
         if(x!=y)
             printf("x=%d y=%d \n",x,y);
         x = x + i; // increment x by i
         y = y - i; // decrement y by i
    }
    return 0;
}
    
