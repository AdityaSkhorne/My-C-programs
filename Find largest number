//program to print the largest of two numbers
#include<stdio.h>
int main (void)
{
  int a,b,max;
  printf("enter values for a and b:- ");
  scanf("%d%d",&a,&b);
  max=a>b?a:b;
  printf("greater from %d and %d is: %d\n",a,b,max);
  return 0;
}  
